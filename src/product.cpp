#include "product.hpp"

Product::Product(std::string name, std::string version, std::string manufacturer): 
                    name(name),
                    version(version),
                    manufacturer(manufacturer)
{

}